<table><tr><td> <em>Assignment: </em> M2 Python-HW</td></tr>
<tr><td> <em>Student: </em> Kshitij Aji (ka598)</td></tr>
<tr><td> <em>Generated: </em> 9/23/2022 1:32:11 PM</td></tr>
<tr><td> <em>Grading Link: </em> <a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IS601-007-F22/m2-python-hw/grade/ka598" target="_blank">Grading</a></td></tr></table>
<table><tr><td> <em>Instructions: </em> <p>Make sure you have the dev/prod branches created before starting this assignment.</p><p>Pre-req Steps if not done so yet:</p><p><ol><li>git checkout main</li><li>git checkout -b dev</li><li>git push origin dev</li><li>git checkout -b prod</li><li>git push origin prod</li></ol><div>This will ensure you have a dev and prod branch on github.</div></p><p>Setup steps:</p><ol><li><code>git checkout dev</code></li><li><code>git pull origin dev</code></li><li><code>git checkout -b M2-Python-HW</code></li></ol><p>You'll have 3 problems to save for this assignment.</p><p>Each problem you're given a template&nbsp;<strong>Do not edit anything in the template except where the comments tell you to</strong>.</p><p>The templates are done in such a way to make it easier to capture the output in a screenshot (if it's still not able to fit well, you can zoom out in your browser).</p><p>You'll copy each template into their own separate .py files, immediately git add, git commit these files (you can do it together) so we can capture the difference/changes between the templates and your additions. This part is required for full credit.</p><p>HW steps:</p><ol><li>Open your IDE at the root of your repository folder</li><li>In your IDE create a new folder/directory called M2</li><li>Create 3 new files in this new M2 folder (problem1.py, problem2.py, problem3.py)</li><li>Paste each template into their respective files</li><li><code>git add .</code></li><li><code>git commit -m "adding template baselines</code></li><li>Do the related work (you may do steps 8 and 9 as often as needed or you can do it all at once at the end)</li><li><code>git add .</code></li><li><code>git commit -m "completed hw"</code></li><li>When you're done push the branch<ol><li><code>git push origin M2-Python-HW</code></li></ol></li><li>Create the Pull Request with&nbsp;<strong>dev</strong>&nbsp;as base and&nbsp;<strong>M2-Python-HW</strong>&nbsp;as compare</li><li>Create a new file in the M2 folder in your IDE called m2_submission.md</li><li>Fill out the below deliverable items, save the submission, and copy to markdown<ol><li>For this assignment you may get screenshots from your IDE output or terminal/console output</li></ol></li><li>Paste the markdown into the m2_submission.md</li><li>add/commit/push the md file<ol><li><code>git add m2_submission.md</code></li><li><code>git commit -m "adding submission file"</code></li><li><code>git push origin M2-Python-HW</code></li></ol></li><li>Merge the pull request from step 11</li><li>On your local machine sync the changes<ol><li><code>git checkout dev</code></li><li><code>git pull origin dev</code></li></ol></li><li>Make a pull request from&nbsp;<strong>prod</strong>&nbsp;as base and&nbsp;<strong>dev</strong>&nbsp;as compare and immediately merge it</li><li>Submit the link to the m2_submission.md file from the prod branch to Canvas</li></ol><p><strong>Template Files</strong>&nbsp;You can find all 3 template files in this gist:&nbsp;<a href="https://gist.github.com/MattToegel/3c55ca7bb631ff6f492bf6f1ad27270e">https://gist.github.com/MattToegel/3c55ca7bb631ff6f492bf6f1ad27270e</a></p></td></tr></table>
<table><tr><td> <em>Deliverable 1: </em> Problem 1 - Only output Odd values of the Array under "Odds output" </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="http://via.placeholder.com/400x120/009955/fff?text=Complete"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Clearly screenshot the output of Problem 1 showing the data</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/113541321/192008253-a3d43936-00e9-451c-a112-860e8ce1fead.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Problem 1 Output<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/113541321/192021240-241e41ed-f473-4aff-88ae-6c225a97cf48.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Problem 1 Code<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>This problem statement asks us to output the odd values from a given<br>list. We use a for loop to iterate through each element in the<br>list. If the element&nbsp;<div>is not divisible by 2, it is odd, and we<br>can print out that element and if the element is divisible by 2<br>we can use the continue statement to move to the&nbsp;</div><div>next iteration of the<br>loop. Divisibility in python can be checked with the modulo operator (%), so<br>if the element in the list, i % 2 is not equal to<br>0, we can</div><div>determine that the element is odd.&nbsp;</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 2: </em> Problem 2 - Only output the sum/total of the list values by assigning it to the 'total' variable (the number must end in 2 decimal places, if it ends in 1 it must have a 0 at the end) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="http://via.placeholder.com/400x120/009955/fff?text=Complete"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Clearly screenshot the output of Problem 2 showing the data</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/113541321/192008400-161ffa5b-c9e4-4310-ba2b-0d445f018bd0.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Problem 2 Output<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/113541321/192021624-f7baa94b-7363-404a-8cfc-612df20586f3.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Problem 2 Code<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>The problem statement wants us to output the sum of all the values<br>in a given list. We can do this by iterating through the list<br>with a for loop.<div>We can set the variable total initially to 0 before<br>the for loop. As we go through each iteration of the loop, we<br>can add total to each element in&nbsp;</div><div>the list. This would give us the<br>sum of all the values in the list. To format the solution to<br>display only two decimal places, we can use the format</div><div>function in python and<br>specify the number of decimal places that needs to be displayed. We can<br>use &quot;{:.2f}&quot;.format(total) to change the format&nbsp;</div><div>of the total variable to only display 2<br>decimal places.&nbsp;</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 3: </em> Problem 3 - Output the given values as positive under the "Positive Output" message (the data otherwise shouldn't change) </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="http://via.placeholder.com/400x120/009955/fff?text=Complete"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Clearly screenshot the output of Problem 3 showing the data</td></tr>
<tr><td><table><tr><td><img width="768px" src="https://user-images.githubusercontent.com/113541321/192008511-3dc5b9b6-d028-4b2d-ad5e-ca0b5b1cf5df.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Problem 3 Output<br></p>
</td></tr>
<tr><td><img width="768px" src="https://user-images.githubusercontent.com/113541321/192021895-d24b0d4c-d428-451e-b8cd-4f72e8d8de6b.png"/></td></tr>
<tr><td> <em>Caption:</em> <p>Problem 3 Code<br></p>
</td></tr>
</table></td></tr>
<tr><td> <em>Sub-Task 2: </em> Describe how you solved the problem</td></tr>
<tr><td> <em>Response:</em> <p>I first created an empty list and called the variable emptyArr. I then<br>used a for loop to iterate through each element in the list. Within<br>the for loop,&nbsp;<div>I used a few conditional statements to handle the different data<br>types. My first if statement was to check if the element was positive<br>and not of the type string,</div><div>it could be appended to the emptyArr variable<br>as is. My next elif statement was to check if the element in<br>the list was a string. If it was a string, I added a<br>nested</div><div>If/ else statement to handle the positive and negative string values. If the<br>float(or int) of the string value was positive, it can be added to<br>the empty array as is but&nbsp;</div><div>if the string value was negative, it had<br>to be converted to a positive integer value by using abs(int(i)) and then<br>converting that to a string by using the string function</div><div>, str() and finally<br>adding the converted positive string value to the emptyArr. My last elif statement<br>was to check if the values were negative and not of the string<br>type.&nbsp;</div><div>In that case, the absolute function was used and the positive values were<br>then added to the emptyArr list.&nbsp;</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td> <em>Deliverable 4: </em> Misc Items </td></tr><tr><td><em>Status: </em> <img width="100" height="20" src="http://via.placeholder.com/400x120/009955/fff?text=Complete"></td></tr>
<tr><td><table><tr><td> <em>Sub-Task 1: </em> Pull Request URL for M2-Python-HW to dev</td></tr>
<tr><td> <a rel="noreferrer noopener" target="_blank" href="https://github.com/Kshitij2126/IS601-007/pull/3">https://github.com/Kshitij2126/IS601-007/pull/3</a> </td></tr>
<tr><td> <em>Sub-Task 2: </em> Talk about what you learned, any issues you had, how you resolve them</td></tr>
<tr><td> <em>Response:</em> <p>In Module 2, we learned about the basics of python such as the<br>syntax, data types, operators, for loops, while loops, and conditional statements such as<br>If, else and elif<div>statements. The homework was directly related to the topics we<br>learned in class. This homework had 3 problems. In the first problem, we<br>had to display only the odd values from a given list. This was<br>done by using a for loop to iterate through each element in the<br>list and by using the modulo operator (%) to check for the divisibility<br>with 2. For the second problem we had to display the sum of<br>the input values as the output and the sum should only display 2<br>decimal places. This was again done with the help of a for loop<br>to iterate through each element of the list and add each value to<br>the total. The format function was used to display the specified number of<br>decimal places. The third problem statement required us to display the positive output<br>for all the elements in the given list. This was a little challenging<br>at first as we had to handle inputs of different data types and<br>the output must be of the same data type as the input. This<br>problem was solved using a for loop and a few nested conditional statements.<br>The absolute function was used to convert the negative values to positive and<br>casting was used to change the string values to int and back to<br>their default string values.</div><br></p><br></td></tr>
</table></td></tr>
<table><tr><td><em>Grading Link: </em><a rel="noreferrer noopener" href="https://learn.ethereallab.app/homework/IS601-007-F22/m2-python-hw/grade/ka598" target="_blank">Grading</a></td></tr></table>
